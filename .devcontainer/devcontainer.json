{
    "name": "PixelCanvas Dev",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.cmake-tools",
                "twxs.cmake"
            ]
        }
    },
    "remoteUser": "vscode",
    // Enable Docker-in-Docker support to run compose from within the dev container
    "features": {
        "ghcr.io/devcontainers/features/docker-in-docker:2": {}
    },
    "postStartCommand": "docker compose up -d"
}